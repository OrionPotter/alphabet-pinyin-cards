name = "alphabet-pinyin-cards"
compatibility_date = "2024-01-01"

[site]
bucket = "."

# Cloudflare Pages 配置
pages_build_output_dir = "."

# 自定义域名（可选，部署后在 Cloudflare Dashboard 配置）
# routes = [
#   { pattern = "example.com", custom_domain = true }
# ]

# 环境变量（如需要）
[env.production]
name = "alphabet-pinyin-cards"

[env.preview]
name = "alphabet-pinyin-cards-preview"
